# ── Secrets & local config — NEVER commit ──
.env
**/.env
config.yaml
config.yaml.*
*.key
*.pem
*.p12
*secret*
*credential*

# ── Backups / scratch ──
*.bak
*bak*
*.pre-*
*.orig
*.tmp
*.tgz

# ── Data / runtime ──
/data
data/
*.db
*.db-shm
*.db-wal
*.log

# ── Build output ──
/homeforge
*.exe
internal/api/webdist/*
!internal/api/webdist/.gitkeep
node_modules/
web/node_modules/
web/build/
web/.svelte-kit/
